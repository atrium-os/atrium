[package]
name = "navigator-style"
version = "0.1.0"
edition = "2021"
description = "M2.1: the Atrium Document Profile's CSS — tokenizer, strict parser, the 64-row property grammar, admitted selectors, diagnostics with positions."

[dependencies]
