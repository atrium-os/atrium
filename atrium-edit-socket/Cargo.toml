[package]
name = "atrium-edit-socket"
version = "0.1.0"
edition = "2021"
description = "Minimal text editor on the Atrium FreeBSD-native stack via fresco-socket-rs"
license = "MIT OR Apache-2.0"
publish = false

[[bin]]
name = "atrium-edit-socket"
path = "src/main.rs"

[dependencies]
fresco-server = { path = "../../fresco-server" }
fresco-socket-rs = { path = "../fresco-socket-rs" }
fresco-text = { path = "../fresco-text" }
