[package]
name        = "tessera-test-harness"
version.workspace     = true
edition.workspace     = true
license.workspace     = true
repository.workspace  = true
description = "In-memory block device + property-test scaffolding for tessera-core."
publish     = false

[lib]
path = "src/lib.rs"

[[bin]]
name = "tessera-property-tests"
path = "src/bin/property_tests.rs"

[dependencies]
tessera-sys = { path = "../tessera-sys" }
