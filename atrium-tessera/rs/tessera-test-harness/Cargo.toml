[package]
name        = "tessera-test-harness"
version.workspace     = true
edition.workspace     = true
license.workspace     = true
repository.workspace  = true
description = "Cross-language test harness for tessera (property + differential)."
publish     = false

[[bin]]
name = "tessera-test-harness"
path = "src/main.rs"

[dependencies]
tessera = { path = "../tessera" }
