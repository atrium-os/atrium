# resources.memory must look like "<positive int><K|M|G>".
# EXPECT_ERROR: must look like
[app]
id      = "org.example.x"
name    = "X"
version = "1.0.0"
entry   = "bin/x"

[resources]
memory = "lots"
