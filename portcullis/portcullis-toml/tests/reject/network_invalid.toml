# network must be one of "none" | "loopback" | "full".
# EXPECT_ERROR: expected "none", "loopback", "full" or a table
[app]
id      = "org.example.x"
name    = "X"
version = "1.0.0"
entry   = "bin/x"

[capabilities]
network = "wifi"
