[package]
name = "atrium-pt-demo"
version = "0.1.0"
edition = "2021"
description = "PT (partial-update transport) demo: whole present then a sub-rect damaged present, to exercise frescod's damage-scissor path."
license = "MIT OR Apache-2.0"
publish = false

[[bin]]
name = "atrium-pt-demo"
path = "src/main.rs"

[dependencies]
fresco-client   = { path = "../fresco-client" }
fresco-protocol = { path = "../fresco-protocol" }
